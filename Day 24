// To see the given number is divisible by 5 and 11
#include <stdio.h>
int main()
{
  int x;
  printf ("no. = ");
  scanf ("%d", &x);

  if (x%5==0 && x%11==0)
  {
    printf("Divisible");
  }

  else
  {
    printf ("not divisible");
  }

  return 0;
}
