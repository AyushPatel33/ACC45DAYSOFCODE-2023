#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);
    
    while (T--) {
        int a, b, x, y, z;
        scanf("%d %d %d", &a, &b, &x);
        scanf("%d %d", &y, &z);
        
        int min_time = ((b - a) / x) + z;
        printf("%d\n", min_time);
    }
    
    return 0;
}
