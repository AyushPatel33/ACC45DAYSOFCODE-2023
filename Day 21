#include <stdio.h>

int main() {
    int T;
    scanf("%d", &T);

    while (T--) {
        int W, X, Y, Z;
        scanf("%d %d %d %d", &W, &X, &Y, &Z);

        int final_balance = W + (X * Z) - (Y * Z);
        printf("%d\n", final_balance);
    }

    return 0;
}
