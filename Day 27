//finding the value of x, y, z and p
#include <stdio.h>

int main()
{
    int x = 10, y = 13, z, p;
    z = x++ > 10 && --y < 12;
    p = y-- > 12 || z++ == 0;
    printf("value of x, y, z, p: %d %d %d %d\n", x, y, z, p);
    return 0;
}
